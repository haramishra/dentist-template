---
import { cn } from "@/lib/utils";
interface Props {
  className?: string;
}
const { className } = Astro.props;
---

<div
  class={cn("grid grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-3", className)}
>
  <slot />
</div>
